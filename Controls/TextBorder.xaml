<UserControl
    x:Class="wzd32.Controls.TextBorder"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:wzd32.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="400"
    d:DesignWidth="600"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/wzd32;component/Resources/Dictionary.xaml" />
            </ResourceDictionary.MergedDictionaries>

        </ResourceDictionary>
    </UserControl.Resources>
    <Border Background="White">
        <Grid Margin="10" ClipToBounds="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <Border
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1"
                ClipToBounds="False"
                CornerRadius="3">
                <Grid Margin="10" ClipToBounds="False">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Margin="0,-20,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        Background="{StaticResource TextBrush}"
                        FontSize="{StaticResource FontSizeCaption}"
                        FontWeight="ExtraLight"
                        Foreground="{StaticResource BorderBrush}"
                        Text="This is a simple example of a blend-in tab control."
                        TextWrapping="Wrap" />
                    <StackPanel
                        Background="{StaticResource TextBrush}"
                        ClipToBounds="False"
                        Orientation="Vertical" />
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>